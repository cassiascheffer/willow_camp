import { Controller } from "@hotwired/stimulus"

// This controller is no longer needed since we're using Turbo Streams
// to update the buttons server-side
export default class extends Controller {
}