<!DOCTYPE html>
<html data-theme="<%= @author&.theme || 'light' %>">
  <head>
    <title><%= content_for(:title) || "willow.camp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <!-- Favicon from OpenMoji assets -->
    <% emoji_filename = emoji_to_openmoji_filename(@author&.favicon_emoji) %>
    <link rel="icon" href="/openmoji-32x32-ico/<%= emoji_filename %>.ico" sizes="32x32">
    <link rel="icon" href="/openmoji-svg-color/<%= emoji_filename %>.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/openmoji-apple-touch-icon-180x180/<%= emoji_filename %>.png">
    <%= stylesheet_link_tag "marksmith" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="font-mono" data-controller="theme" data-theme-theme-value="<%= @author&.theme || 'light' %>">
    <nav class="bg-base-100 shadow-md w-full px-8" aria-label="Main Navigation">
      <!-- Mobile menu with details/summary -->
      <details class="lg:hidden">
        <summary class="py-4 flex items-center justify-between cursor-pointer">
          <a class="text-xl font-bold hover:text-primary transition-colors" href="<%= dashboard_path %>"><%= blog_title_for(@user) %></a>
          <%= heroicon "bars-3", options: { class: "h-6 w-6" } %>
        </summary>
        <div class="pb-4 space-y-2">
          <% if @blog %>
            <%= link_to "Posts", blog_dashboard_path(@blog.subdomain), class: "block link link-hover font-medium py-2 #{current_page?(blog_dashboard_path(@blog.subdomain)) ? 'text-primary' : ''}" %>
            <%= link_to "Tags", blog_dashboard_tags_path(@blog.subdomain), class: "block link link-hover font-medium py-2 #{current_page?(blog_dashboard_tags_path(@blog.subdomain)) ? 'text-primary' : ''}" %>
            <%= link_to "Settings", blog_dashboard_settings_path(@blog.subdomain), class: "block link link-hover font-medium py-2 #{current_page?(blog_dashboard_settings_path(@blog.subdomain)) ? 'text-primary' : ''}" %>
          <% else %>
            <%= link_to "Posts", dashboard_path, class: "block link link-hover font-medium py-2 #{current_page?(dashboard_path) ? 'text-primary' : ''}" %>
            <%= link_to "Tags", dashboard_tags_path, class: "block link link-hover font-medium py-2 #{current_page?(dashboard_tags_path) ? 'text-primary' : ''}" %>
            <%= link_to "Settings", dashboard_settings_path, class: "block link link-hover font-medium py-2 #{current_page?(dashboard_settings_path) ? 'text-primary' : ''}" %>
          <% end %>
          <hr class="my-2">
          <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'block link link-hover font-medium py-2', role: 'button', tabindex: '0' %>
        </div>
      </details>
      <!-- Desktop menu -->
      <div class="hidden lg:flex lg:items-center lg:justify-between py-4">
        <a class="text-xl font-bold hover:text-primary transition-colors" href="<%= dashboard_path %>"><%= blog_title_for(@user) %></a>
        <div class="space-x-4">
          <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'link link-hover font-medium', role: 'button', tabindex: '0' %>
        </div>
      </div>
    </nav>
    <main class="max-w-6xl mx-auto p-4" aria-label="Dashboard Content">
      <header aria-label="Dashboard Header" class="flex justify-between items-center mb-6">
        <div class="flex items-center gap-4">
          <div role="tablist" class="tabs tabs-boxed hidden lg:flex">
            <% if @blog %>
              <%= link_to "Posts", blog_dashboard_path(@blog.subdomain), class: "tab #{current_page?(blog_dashboard_path(@blog.subdomain)) ? 'tab-active' : ''}" %>
              <%= link_to "Tags", blog_dashboard_tags_path(@blog.subdomain), class: "tab #{current_page?(blog_dashboard_tags_path(@blog.subdomain)) ? 'tab-active' : ''}" %>
              <%= link_to "Settings", blog_dashboard_settings_path(@blog.subdomain), class: "tab #{current_page?(blog_dashboard_settings_path(@blog.subdomain)) ? 'tab-active' : ''}" %>
            <% else %>
              <%= link_to "Posts", dashboard_path, class: "tab #{current_page?(dashboard_path) ? 'tab-active' : ''}" %>
              <%= link_to "Tags", dashboard_tags_path, class: "tab #{current_page?(dashboard_tags_path) ? 'tab-active' : ''}" %>
              <%= link_to "Settings", dashboard_settings_path, class: "tab #{current_page?(dashboard_settings_path) ? 'tab-active' : ''}" %>
            <% end %>
          </div>
          <!-- Blog Switcher Dropdown -->
          <div class="dropdown dropdown-end">
            <div tabindex="0" role="button" class="btn btn-sm btn-outline" aria-label="Switch blog">
              <span class="hidden sm:inline"><%= @blog&.title || @blog&.subdomain || "Select Blog" %></span>
              <span class="sm:hidden">Blog</span>
              <%= heroicon "chevron-down", variant: :mini, options: { class: "h-4 w-4" } %>
            </div>
            <ul tabindex="0" class="dropdown-content menu bg-base-100 rounded-box z-[1] w-64 p-2 shadow-lg border">
              <li class="menu-title">
                <span>Your Blogs</span>
              </li>
              <% current_user.blogs.order(:title).each do |blog| %>
                <li>
                  <% if blog == @blog %>
                    <span class="flex items-center justify-between text-primary">
                      <div>
                        <div class="font-medium"><%= blog.title || blog.subdomain %></div>
                        <div class="text-xs opacity-70"><%= blog.subdomain %>.willow.camp</div>
                      </div>
                      <%= heroicon "check", variant: :mini, options: { class: "h-4 w-4" } %>
                    </span>
                  <% else %>
                    <a href="<%= blog_dashboard_path(blog.subdomain) %>" class="flex items-center justify-between">
                      <div>
                        <div class="font-medium"><%= blog.title || blog.subdomain %></div>
                        <div class="text-xs opacity-70"><%= blog.subdomain %>.willow.camp</div>
                      </div>
                    </a>
                  <% end %>
                </li>
              <% end %>
              <div class="divider my-2"></div>
              <li>
                <%= link_to dashboard_path(new_blog: true), class: "text-primary flex items-center gap-2" do %>
                  <%= heroicon "plus", variant: :mini, options: { class: "h-4 w-4" } %>
                  Start a new blog
                <% end %>
              </li>
            </ul>
          </div>
        </div>
        <div class="flex gap-2">
          <% if @blog&.subdomain.present? %>
            <%= button_to "New Post", dashboard_untitled_posts_path(blog_subdomain: @blog.subdomain), method: :post, class: "btn btn-sm btn-primary" %>
            <a href="<%= posts_url(subdomain: @blog.subdomain) %>" class="btn btn-sm btn-outline" target="_blank">View Site</a>
          <% end %>
        </div>
      </header>
      <%= yield %>
    </main>
    <footer class="footer p-4 bg-base-200 text-base-content" aria-label="Footer">
      <div class="container mx-auto flex gap-4">
        <p>willow.camp</p>
        <p class="text-sm text-base-content/60"><%= link_to "Terms of Service", terms_path, class: "link hover:link-primary" %></p>
        <p class="text-sm text-base-content/60"><%= link_to "Status", "https://status.willow.camp/", class: "link hover:link-primary", target: "_blank", rel: "noopener" %></p>
        <p class="text-sm text-base-content/60"><%= link_to "Discord", "https://discord.gg/g4KUdZaf", class: "link hover:link-primary", target: "_blank", rel: "noopener" %></p>
      </div>
    </footer>
    <div id="flash-messages" class="toast toast-bottom toast-end z-50">
      <%= render 'shared/flash_messages' %>
    </div>
  </body>
</html>
