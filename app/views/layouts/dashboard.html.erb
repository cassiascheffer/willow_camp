<!DOCTYPE html>
<html data-theme="" data-controller="">
  <head>
    <title><%= content_for(:title) || "willow.camp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <% if Rails.env.local? %>
      <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22 transform=%22rotate(180,50,50)%22>⛺</text></svg>">
  <% else %>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>⛺</text></svg>">
<% end %>
<%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
<%= javascript_importmap_tags %>
</head>
<body class="font-mono">
  <div id="flash-messages" class="fixed top-0 left-0 right-0 z-50 transform transition-transform duration-300">
    <% if notice %>
      <div data-controller="flash" class="alert alert-success shadow-lg" role="alert" aria-live="polite">
        <div>
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
          <span><%= notice %></span>
        </div>
        <button class="btn btn-sm btn-circle btn-outline ml-4" data-action="click->flash#dismiss" aria-label="Dismiss notice">&times;</button>
      </div>
    <% end %>
    <% if alert %>
      <div data-controller="flash" class="alert alert-error shadow-lg" role="alert" aria-live="assertive">
        <div>
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
          <span><%= alert %></span>
        </div>
        <button class="btn btn-sm btn-circle btn-outline ml-4" data-action="click->flash#dismiss" aria-label="Dismiss alert">&times;</button>
      </div>
    <% end %>
  </div>
  <nav class="navbar bg-base-100 shadow-md" aria-label="Main Navigation">
    <div class="flex-1">
      <a class="btn btn-ghost normal-case text-xl" href="<%= dashboard_path %>"><%= blog_title_for(@user) %></a>
    </div>
    <div class="flex-none">
      <a class="btn btn-primary" href="<%= dashboard_settings_path %>">Settings</a>
    </div>
  </nav>
  <main class="max-w-6xl mx-auto p-4" aria-label="Dashboard Content">
    <%= yield %>
  </main>
  <footer class="footer p-4 bg-base-200 text-base-content" aria-label="Footer">
    <div class="container mx-auto text-center">
      <p>willow.camp</p>
    </div>
  </footer>
</body>
</html>
