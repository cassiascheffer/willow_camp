<!DOCTYPE html>
<html data-theme="<%= @user&.theme || "tokyo" %>">
  <head>
    <title><%= content_for(:title) || "willow.camp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>⛺</text></svg>">
    <!-- <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png"> -->

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div id="flash-messages" class="fixed top-0 left-0 right-0 z-50 transform transition-transform duration-300">
      <% if notice %>
        <div data-controller="flash" class="flash flash-notice p-4 bg-green-100 border-l-4 border-green-500 text-green-700 mb-2 flex justify-between items-center">
          <div><%= notice %></div>
          <button class="flash-close ml-4" data-action="click->flash#dismiss">&times;</button>
        </div>
      <% end %>

      <% if alert %>
        <div data-controller="flash" class="flash flash-alert p-4 bg-red-100 border-l-4 border-red-500 text-red-700 mb-2 flex justify-between items-center">
          <div><%= alert %></div>
          <button class="flash-close ml-4" data-action="click->flash#dismiss">&times;</button>
        </div>
      <% end %>
    </div>

    <nav class="bg-card shadow-md">
      <div class="container mx-auto px-5 md:px-8 py-3 flex items-center justify-between">
      <div class="text-lg font-bold text-accent">
        <%= link_to(@user.blog_title || "#{@user.subdomain}.willow.camp", dashboard_path, class: "text-accent") %>
      </div>
      <div>
        <%= link_to "Settings", dashboard_settings_path, class: "text-accent hover:text-[#005273] dark:hover:text-[#5D7FE3]" %>
      </div>
      </div>
    </nav>

    <main class="container-standard">
      <%= yield %>
    </main>

    <footer class="border-t border-[#E5E7EB] dark:border-[#3A3D4D] py-4 px-5 mt-12">
      <div class="container mx-auto text-sm text-muted">
        &copy; <%= Time.current.year %> Willow Camp
      </div>
    </footer>
  </body>
</html>
