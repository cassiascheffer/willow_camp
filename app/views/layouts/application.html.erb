<!DOCTYPE html>
<html data-theme=<%= @author&.theme || "tokyo" %>
  <head>
    <title><%= content_for(:title) || "Willow Camp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% unless controller_name == 'home' && action_name == 'show' || controller_name == 'sessions' && action_name == 'new' %>
      <header class="border-b border-[#E5E7EB] dark:border-[#3A3D4D] py-4 px-5">
      <div class="container mx-auto flex justify-between items-center">
      <a href="/" class="text-xl font-bold text-accent"><%= @author&.blog_title || "#{@author.subdomain}.willow.camp" %></a>
      <nav class="space-x-4">
        <a href="/" class="bg-hover">Home</a>
        <a href="/posts" class="bg-hover">Posts</a>
      </nav>
      </div>
      </header>
    <% end %>
    
    <main class="container-narrow">
      <%= yield %>
    </main>
    
    <footer class="border-t border-[#E5E7EB] dark:border-[#3A3D4D] py-4 px-5 mt-12">
      <% if @author.present? %>
        <div class="container mx-auto text-sm text-muted">
          &copy; <%= Time.current.year %> <%= @author.blog_title || "#{@author.subdomain}.willow.camp" %> | Made with <%= link_to "willow.camp", root_url %>
        </div>
      <% end %>
    </footer>
  </body>
</html>
