<% content_for :title, "New post" %>

<div class="container-narrow">
  <div class="tokyo-card">
    <h1 class="font-bold text-2xl mb-6">New post</h1>

    <%= render "form", post: @post %>

    <div class="mt-4">
      <%= link_to "Back to posts", posts_path, class: "tokyo-button-secondary" %>
    </div>
  </div>
</div>
