# Database connection (same as Rails DATABASE_URL)
DATABASE_URL=postgresql://username:password@localhost:5432/willow_camp_development

# Server port
PORT=3001

# Session secret (generate with: openssl rand -base64 32)
# SESSION_SECRET=your-secret-here
