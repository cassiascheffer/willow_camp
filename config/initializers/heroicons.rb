# Configure the heroicons gem
Heroicons.configure do |config|
  # Set the default variant to outline instead of solid
  config.variant = :outline

  # Icon morphing is disabled by default
  # config.morph = true
end
